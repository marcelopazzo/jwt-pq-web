# Loaded by Kamal on deploy. Values come from the CI environment (GitHub
# Secrets). For local invocations, export the variables in your shell or
# pipe them from a password manager before running `kamal`.

KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
RAILS_MASTER_KEY=$RAILS_MASTER_KEY
